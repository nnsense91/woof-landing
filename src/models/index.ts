export type TProduct = {
  brand: string;
  category: string;
  description: string;
  discountPercentage: number;
  id: number;
  price: number
  rating: number;
  stock: number;
  thumbnail: string;
  title: string;
}

export type TWoof = string;